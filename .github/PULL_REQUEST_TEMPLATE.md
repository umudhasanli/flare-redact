## What this changes

<!-- One or two sentences. What's different after this merges? -->

## Why

<!-- The problem it solves, or the issue it closes (Fixes #123). -->

## How I checked it

<!-- Tests, a CLI run, a benchmark — whatever proves it works. -->

---

- [ ] Tests cover the change (a match *and* a near-miss, if it's a detector)
- [ ] No new runtime dependencies
- [ ] `npm run build && node --test` passes locally
